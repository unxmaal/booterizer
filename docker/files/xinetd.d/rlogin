service login
{
    socket_type     = stream
    protocol        = tcp
    wait            = no
    user            = root
    server          = /usr/sbin/in.rlogind
    log_on_failure  += USERID
    disable         = no
}
